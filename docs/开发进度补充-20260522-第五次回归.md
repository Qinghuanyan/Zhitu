# 2026-05-22 第五次回归补充

## 1. 本轮目标

继续按“逐项检查 + 真机回归 + 同步文档”的要求，重点处理以下链路：

- `replan_trip`
- `export_itinerary`
- `share_itinerary`
- activity 推荐为空的补强

---

## 2. 本轮代码修复

### 2.1 修复 `replan_trip` 会清空酒店/餐饮推荐

修改文件：

- `app/src/main/java/me/rerere/rikkahub/service/ChatService.kt`

修复思路：

- 当本次重规划没有拿到新的 `hotels / foods / activities` 时，不再直接用空列表覆盖；
- 优先使用：
  1. 本次 payload
  2. `planningFacts`
  3. 当前会话 `currentPlan` 里已有的推荐

结果：

- 避免了“重规划成功，但酒店/美食卡片被清空”的问题。

### 2.2 增加由 POI 回填推荐卡片的 fallback

同样修改：

- `app/src/main/java/me/rerere/rikkahub/service/ChatService.kt`

补强逻辑：

- 如果推荐列表仍为空，但 `payload.pois` / `currentPlan.pois` 里已经有同类别 POI；
- 则从这些 POI 回填出推荐卡片；
- 当前主要用于 activity 类别，避免：
  - 地图里有景点
  - itinerary/poi 有数据
  - 但 activity tab 为空

说明：

- 这层逻辑已经编译通过；
- 但在应用重启后的新 WebView page 上，DevTools `Runtime.evaluate` 没有稳定返回，所以这一补强还缺最后一次同会话真机复验。

---

## 3. 本轮已完成的真机验证

设备：

- `emulator-5554`

### 3.1 `replan_trip`：酒店/餐饮不再被清空

测试会话：

- `conversationId = 7a158390-431b-43ec-a83f-3a5975c2e038`

重规划前已观察到旧问题：

- `itineraryDays = 2`
- `hotels = 0`
- `foods = 0`
- `activities = 0`
- `pois = 10`

修复后再次触发 `replan_trip`，HTML state dump 结果：

- `itineraryDays = 2`
- `hotels = 6`
- `foods = 6`
- `activities = 0`
- `pois = 10`

结论：

- **酒店推荐已恢复**
- **美食推荐已恢复**
- activity 当时仍为 0，因此本轮又补了一层“从 POI 回填 activity 推荐”的 fallback

### 3.2 `export_itinerary`：真实生成 Markdown

验证方式：

- 触发 HTML 动作 `export_itinerary`
- 用 `adb shell run-as me.rerere.rikkahub.debug ls -l cache/zhitu-exports`

结果：

- 成功生成：
  - `cache/zhitu-exports/上海市.md`

抽样内容确认：

- 文件内包含：
  - 行程摘要
  - 每日安排
  - 酒店推荐
  - 美食推荐

说明：

- 导出不是空壳；
- 是真实把当前行程转成 Markdown 落盘。

### 3.3 `share_itinerary`：真实弹出系统分享面板

验证方式：

- 触发 HTML 动作 `share_itinerary`
- 检查前台窗口

结果：

- 当前焦点窗口为：
  - `android/com.android.internal.app.ChooserActivity`

结论：

- 分享链路已经真实打到系统 chooser。

---

## 4. 本轮构建结果

使用：

- `Zhitu/.tools/gradle-9.1.0/bin/gradle.bat :app:assembleDebug`

结果：

- `BUILD SUCCESSFUL`

---

## 5. 当前状态判断

### 本轮已确认可用

- `replan_trip` 不再把 hotel / food 清空
- `export_itinerary` 可真实导出 Markdown
- `share_itinerary` 可真实打开系统分享面板

### 本轮已修但仍待补最后一次复验

- activity 推荐从 `poi` 回填的 fallback

未完成最终复验的原因不是代码未通过，而是：

- 应用重启后新的 WebView DevTools page 可以建立连接；
- 但对新 attached page 的 `Runtime.evaluate` 没有稳定返回结果；
- 导致没能在同一轮里再完整 dump 新历史会话的最终 state。

---

## 6. 下一步建议

下一轮优先顺序：

1. 重新进入历史会话 `7a158390-431b-43ec-a83f-3a5975c2e038`
2. 再次触发 `replan_trip`
3. 确认：
   - `activities > 0`
   - activity tab 已显示真实卡片
4. 补做一次消息发送 / 新生成场景回归
5. 若都稳定，再决定是否进一步清理旧 `TravelHubPage.kt`
