# 页面返回入口审计 - 2026-05-23

## 1. 审计范围
- Android Compose 页面：`app/src/main/java/me/rerere/rikkahub/ui/pages/**/*Page.kt`
- TravelHub HTML 页面：`Zhitu/ai_travel_planner.html`

## 2. 审计方法
- 静态扫描所有 `*Page.kt` 是否存在显式 `BackButton()`
- 抽样检查常用二级页的 `TopAppBar` / `LargeFlexibleTopAppBar` 返回入口
- 检查 TravelHub HTML 全屏 / 叠层是否存在可见关闭入口

## 3. 本轮结论

### 3.1 已补齐的缺失返回入口
1. `app/src/main/java/me/rerere/rikkahub/ui/pages/developer/DeveloperPage.kt`
   - 已为 `TopAppBar` 补充 `navigationIcon = { BackButton() }`
2. `app/src/main/java/me/rerere/rikkahub/ui/pages/share/handler/ShareHandlerPage.kt`
   - 已为 `TopAppBar` 补充 `navigationIcon = { BackButton() }`

### 3.2 已确认具备返回入口的页面样本
- `app/src/main/java/me/rerere/rikkahub/ui/pages/chat/ChatMapPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/history/HistoryPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/setting/SettingPage.kt`
- 其他多数二级页已通过 `BackButton()` 统一接入返回入口

### 3.3 静态扫描未命中 `BackButton()` 的页面
- `app/src/main/java/me/rerere/rikkahub/ui/pages/chat/ChatPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/design/DesignHtmlPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/travel/TravelHubHtmlPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/travel/TravelHubPage.kt`

说明：
- 以上 4 个页面目前归类为主入口页 / 容器页，不按“二级页必须显示 BackButton”验收。
- 后续仍建议做一次运行态回归，确认这些主入口页在抽屉、WebView 内叠层、外部跳转返回时没有死路。

## 4. TravelHub HTML 关闭入口审计

### 4.1 已确认存在可见关闭按钮的叠层
- `#map-fullscreen-close`
- `#itinerary-overlay-close`
- `#preference-sheet-close`
- `#profile-overlay-close`

### 4.2 已确认存在显式关闭逻辑的叠层
- 个人中心：`close-profile`
- 行程全屏层：`close-itinerary-overlay`

### 4.3 本轮建议
- `map-fullscreen-close` 与 `preference-sheet-close` 已在 HTML 中存在可见关闭按钮，建议补一轮真机 / 模拟器点击回归，确认点击路径与宿主状态同步完全正常。

## 5. 当前审计结论
- 本轮已补齐静态扫描发现的 Compose 二级页缺失返回入口问题。
- TravelHub HTML 主要叠层均已具备可见关闭控件。
- 剩余工作重点不是继续补按钮，而是做一次运行态导航回归，覆盖：
  - 历史会话 -> 会话页
  - 分享入口 -> 聊天页
  - 地图全屏 -> 原聊天 / TravelHub
  - 个人中心 / 行程全屏 / 偏好面板的打开与关闭
