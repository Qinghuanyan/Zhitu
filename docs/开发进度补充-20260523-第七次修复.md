# 2026-05-23 第七次修复

## 1. 本轮落地项

### 1.1 生成中状态持久化 / 恢复
- `app/src/main/java/me/rerere/rikkahub/service/ChatService.kt`
  - 新增生成中会话节流持久化：
    - `progressPersistJobs`
    - `progressPersistAt`
    - `persistConversationProgress(...)`
  - `initializeConversation(...)` 改为优先保留仍在生成的内存 session，避免历史页往返时被旧库状态覆盖。
  - 新增中断生成恢复逻辑，防止会话重新打开后一直停留在错误的“生成中”状态。

### 1.2 单模型 3 分钟超时 + 自动切换备用模型
- `app/src/main/java/me/rerere/rikkahub/data/ai/GenerationHandler.kt`
  - 新增 `PER_MODEL_GENERATION_TIMEOUT_MS = 180_000L`
  - 流式生成与单次文本生成都接入 `withTimeout(...)`
  - 新增 `GenerationFallbackNotice`
- `app/src/main/java/me/rerere/rikkahub/service/ChatService.kt`
  - 流式回复 fallback 时向界面写入明确提示
  - 标题生成、建议生成、旅行文本规划链路统一复用 fallback 逻辑

### 1.3 全页面返回入口审计
- 新增审计文档：`docs/页面返回入口审计-20260523.md`
- 已补齐：
  - `app/src/main/java/me/rerere/rikkahub/ui/pages/developer/DeveloperPage.kt`
  - `app/src/main/java/me/rerere/rikkahub/ui/pages/share/handler/ShareHandlerPage.kt`

### 1.4 定位文案修正
- `app/src/main/java/me/rerere/rikkahub/ui/pages/chat/ChatMapDrawer.kt`
  - “设备定位”改为“当前位置”
  - 优先显示真实 `poiName` / `address`
  - 仅在地点语义缺失时回退为“当前位置 + 经纬度”

## 2. 验证结果
- 已执行：`.\.tools\gradle-9.1.0\bin\gradle.bat :app:compileDebugKotlin`
- 结果：`BUILD SUCCESSFUL`

## 3. 本轮产出文件
- `app/src/main/java/me/rerere/rikkahub/data/ai/GenerationHandler.kt`
- `app/src/main/java/me/rerere/rikkahub/service/ChatService.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/chat/ChatMapDrawer.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/developer/DeveloperPage.kt`
- `app/src/main/java/me/rerere/rikkahub/ui/pages/share/handler/ShareHandlerPage.kt`
- `docs/页面返回入口审计-20260523.md`

## 4. 后续建议回归
- AI 长回复过程中切历史页再返回，确认回复继续增长且最终能正常结束
- 主模型故意超时 / 断开时，确认 3 分钟内切换到备用模型
- 分享页、开发者页、地图页、TravelHub 叠层做一轮真机返回路径回归
