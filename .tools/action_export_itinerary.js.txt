(function () {
  const btn = document.querySelector('[data-zhitu-action="export_itinerary"]');
  if (!btn) return "missing-export-itinerary";
  btn.click();
  return JSON.stringify({
    currentTab: window.__ZHITU_STATE__ && window.__ZHITU_STATE__.currentTab,
    title: document.title,
  });
})()
