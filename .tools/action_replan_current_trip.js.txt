(function () {
  const btn = document.querySelector('[data-zhitu-action="replan_trip"]');
  if (!btn) return "missing-replan-trip";
  btn.click();
  return JSON.stringify({
    currentTab: window.__ZHITU_STATE__ && window.__ZHITU_STATE__.currentTab,
    title: document.title,
  });
})()
