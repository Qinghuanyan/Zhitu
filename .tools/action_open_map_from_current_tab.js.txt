(function () {
  const btn = document.querySelector('[data-zhitu-action="open_map"]');
  if (!btn) return "missing-open-map";
  btn.click();
  return JSON.stringify({
    currentTab: window.__ZHITU_STATE__ && window.__ZHITU_STATE__.currentTab,
    title: document.title,
  });
})()
