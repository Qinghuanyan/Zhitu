(function () {
  const btn = document.querySelector('.quick-tab[data-tab="itinerary"]');
  if (!btn) return "missing-itinerary-tab";
  btn.click();
  const chat = document.getElementById("chat-container");
  return JSON.stringify({
    currentTab: window.__ZHITU_STATE__ && window.__ZHITU_STATE__.currentTab,
    chatText: (chat && chat.innerText || "").slice(0, 1000),
  });
})()
