JSON.stringify((function () {
  const state = window.__ZHITU_STATE__ || {};
  return {
    currentTab: state.currentTab,
    conversationId: state.conversation && state.conversation.id,
    messageCount: state.conversation && state.conversation.messages ? state.conversation.messages.length : 0,
    isGenerating: state.conversation && state.conversation.isGenerating,
    travelPlanStatus: state.travelPlan && state.travelPlan.status,
    itineraryDays: state.travelPlan && state.travelPlan.itineraryDays ? state.travelPlan.itineraryDays.length : 0,
    hotels: state.travelPlan && state.travelPlan.hotels ? state.travelPlan.hotels.length : 0,
    foods: state.travelPlan && state.travelPlan.foods ? state.travelPlan.foods.length : 0,
    activities: state.travelPlan && state.travelPlan.activities ? state.travelPlan.activities.length : 0,
    pois: state.travelPlan && state.travelPlan.pois ? state.travelPlan.pois.length : 0,
    searchQuery: state.travelUiState && state.travelUiState.searchQuery,
    availableActions: state.availableActions,
  };
})())
